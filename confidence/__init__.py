"""The confidence gate — addendum section B.

geo/validate.py's threshold table is the PRIMARY path and stays permanently.
This package holds the flag-selected upgrade: an L2-regularised logistic
regression fitted on the spec 11 benchmark.

Addendum B.4's non-negotiable, restated because it is easy to forget at hour 33:
"Ship spec 9.1's threshold table. It works. The learned gate is an upgrade to a
working system, never a dependency."
"""
