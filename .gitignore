.venv/
.venv-ml/
__pycache__/
*.py[cod]
.pytest_cache/
.cache/
data/
node_modules/
web/dist/
*.ckpt
*.pth
*.safetensors
*.glb
*.ply
.env
scratch_*.py
# Source photographs: often someone else's copyright, and large. Keep local.
photos/

# The footprint cache IS committed — it is small, and it is what saves the demo
# when Overpass rate-limits mid-judging (spec 3.2, failure mode 12).
!fixtures/footprints/

# Generated artifacts (confidence/train.py, perception/segment.py outputs)
outputs/

# A 61 MB downloaded asset used for the "place any model" demo. Not ours to
# redistribute and too big for git; see HANDOVER.md for how to reproduce it.
building-buildify-nyc/
